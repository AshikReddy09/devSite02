Options -MultiViews
RewriteEngine On

# Let Vite assets and existing files load normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route client-side navigation to the React entry point.
RewriteRule ^ index.html [L]
